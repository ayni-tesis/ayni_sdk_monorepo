"use client";

import { IconArrowRight, IconChevronDown, IconPlus, IconSettings } from "@tabler/icons-react";
import { useState } from "react";
import { AppSidebar } from "@/components/app-sidebar";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { SidebarInset, SidebarProvider, SidebarTrigger } from "@/components/ui/sidebar";

type App = { name: string; detail: string; status: string };
const initialApps: App[] = [{ name: "Cacao Vision Mobile", detail: "2 workflows · 3 modelos", status: "Sincronizado" }, { name: "Inspector de cultivos", detail: "1 workflow · sin publicar", status: "Borrador" }];

export default function Dashboard({ userName }: { userName: string }) {
  const [apps, setApps] = useState(initialApps); const [creating, setCreating] = useState(false); const [name, setName] = useState("");
  const create = () => { const value = name.trim(); if (!value) return; setApps((items) => [...items, { name: value, detail: "Aún sin workflows", status: "Configuración" }]); setName(""); setCreating(false); };
  return <SidebarProvider><AppSidebar /><SidebarInset><header className="flex h-16 items-center justify-between border-b px-4"><div className="flex items-center gap-2"><SidebarTrigger /><span className="text-sm text-muted-foreground">Workspace</span><span className="font-medium">Laboratorio Andino</span><IconChevronDown className="size-4 text-muted-foreground" /></div><div className="flex items-center gap-3 text-sm"><span className="hidden text-muted-foreground sm:inline">Hola, {userName}</span><span className="size-8 rounded-full bg-secondary text-center leading-8">{userName.slice(0, 1).toUpperCase()}</span></div></header><main className="mx-auto w-full max-w-6xl p-6 md:p-10"><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-sm text-primary">CONFIGURACIÓN DEL WORKSPACE</p><h1 className="text-3xl font-semibold">Tus aplicaciones</h1><p className="mt-2 max-w-xl text-muted-foreground">Cada aplicación mantiene aislados sus credenciales, modelos y workflows que luego se sincronizan al SDK Flutter.</p></div><Button onClick={() => setCreating(true)}><IconPlus />Crear aplicación</Button></div>{creating && <Card className="mb-6 border-primary/50"><CardContent className="flex flex-col gap-3 p-4 sm:flex-row"><Input autoFocus placeholder="Nombre de la aplicación" value={name} onChange={(event) => setName(event.target.value)} onKeyDown={(event) => event.key === "Enter" && create()} /><Button onClick={create}>Crear</Button><Button variant="ghost" onClick={() => setCreating(false)}>Cancelar</Button></CardContent></Card>}<section className="grid gap-4 md:grid-cols-2">{apps.map((app) => <Card key={app.name} className="transition-colors hover:bg-accent/40"><CardHeader><div className="flex items-start justify-between"><div><CardTitle>{app.name}</CardTitle><CardDescription className="mt-2">{app.detail}</CardDescription></div><span className="rounded-full bg-primary/15 px-2 py-1 text-xs text-primary">{app.status}</span></div></CardHeader><CardContent className="flex items-center justify-between border-t pt-4"><span className="font-mono text-xs text-muted-foreground">app_{app.name.toLowerCase().replaceAll(" ", "_")}</span><Button variant="ghost" size="sm">Configurar <IconArrowRight /></Button></CardContent></Card>)}<button onClick={() => setCreating(true)} className="flex min-h-44 flex-col items-center justify-center rounded-lg border border-dashed border-border text-muted-foreground transition-colors hover:border-primary hover:bg-accent/30 hover:text-foreground"><IconPlus className="mb-2" />Crear una nueva aplicación</button></section><section className="mt-10 grid gap-4 md:grid-cols-3"><Card><CardHeader><CardTitle className="text-base">Primer workflow</CardTitle><CardDescription>Define un DAG y valida sus nodos antes de publicar.</CardDescription></CardHeader></Card><Card><CardHeader><CardTitle className="text-base">Modelo on-device</CardTitle><CardDescription>Asocia versiones TensorFlow Lite a tu aplicación.</CardDescription></CardHeader></Card><Card><CardHeader><CardTitle className="text-base">Privacidad</CardTitle><CardDescription>Configura telemetría y captura de evidencia con consentimiento.</CardDescription></CardHeader></Card></section></main></SidebarInset></SidebarProvider>;
}
