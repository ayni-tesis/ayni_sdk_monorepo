name: ayni_sdk
description: Offline-first SDK primitives for Ayni Flutter integrations.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  test: ^1.25.0
